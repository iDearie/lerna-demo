{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna run tsc && lerna publish --no-verify-access"
  },
  "devDependencies": {
    "lerna": "^3.13.1",
    "typescript": "^3.4.4"
  }
}
